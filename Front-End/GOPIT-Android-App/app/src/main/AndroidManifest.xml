<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imageclassificationdemo">
    <!-- camera access -->
    <uses-feature
        android:name="android.hardware.Camera"
        android:required="true" />
    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.imageclassificationdemo.Help"></activity>
        <activity android:name="com.example.imageclassificationdemo.AboutUs" />
        <activity android:name="com.example.imageclassificationdemo.result_window" />
        <activity android:name="com.example.imageclassificationdemo.selectOptionActivity" />
        <activity android:name="com.example.imageclassificationdemo.ScanActivity" />
        <activity android:name="com.example.imageclassificationdemo.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>